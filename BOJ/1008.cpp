#include <bits/stdc++.h>
using namespace std;

int main(void)
{
    // freopen("input.txt", "r", stdin);
    ios::sync_with_stdio(0);
    cin.tie(0);

    double a, b;
    cin >> a >> b;

    cout << fixed;
    cout.precision(9);
    cout << a / b;
}